# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (keep secrets out of git; commit .env.example instead)
.env
.env.*
!.env.example

# Source 3D asset packs — the app only ships public/robot/RobotAnimated.fbx.
# Kept out of git so they don't bloat the repo (asset-ripper is ~1.2 GB).
/robot/
/asset-ripper/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
